# Uncompose

Local-first, open-source music source separation.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues on `thedahm/uncompose`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, using their default label strings. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
