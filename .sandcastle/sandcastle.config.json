{
  "repoChecks": "`cargo test` at the repo root runs everything Rust-side, plus `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` (CI parity, see CONTRIBUTING.md). If the touched code includes the engine shim, run `uv run pytest` in `engine/`.",
  "copyToWorktree": [
    "node_modules",
    "target",
    "engine/.venv"
  ],
  "roster": {
    "haiku": {
      "provider": "claudeCode",
      "model": "claude-haiku-4-5"
    },
    "sonnet": {
      "provider": "claudeCode",
      "model": "claude-sonnet-5"
    },
    "opus": {
      "provider": "claudeCode",
      "model": "claude-opus-5"
    },
    "fable": {
      "provider": "claudeCode",
      "model": "claude-fable-5"
    }
  },
  "implementerDefaults": {
    "tier": "opus",
    "effort": "high"
  }
}
